// PRSync ADO extension entry point.
//
// This panel renders inside the Azure DevOps pull request page as a
// contributed section. Implementation follows the component layout
// spec in docs/handoff/panel-layout-spec.md.
//
// Left intentionally empty until the tdd -> build phase of the
// grill-me -> write-a-prd -> ... pipeline (see .claude/CLAUDE.md).

export {};
