{
  "name": "@prsync/bot",
  "private": true,
  "version": "0.1.0",
  "description": "PRSync Teams Bot Framework bot - required from v1 for personal 1:1 DMs (a Teams incoming webhook can't message individual users). v1 sends static, non-interactive Adaptive Cards; interactive card actions are deferred to v2.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "package": "cd teams && bestzip ../prsync-teams.zip *"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "bestzip": "^3.0.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@azure/data-tables": "^13.3.2",
    "@azure/functions": "^4.16.2",
    "botbuilder": "^4.23.3"
  }
}
