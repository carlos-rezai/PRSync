// PRSync Azure Functions API entry point.
//
// HTTP entry points live under src/functions/ and delegate to the
// service layer (src/services/), which is the only caller of the
// storage layer (src/storage/). Pure helpers live in src/lib/. See
// the layer responsibilities table in .claude/CLAUDE.md.
//
// Left intentionally empty until the tdd -> build phase of the
// grill-me -> write-a-prd -> ... pipeline (see .claude/CLAUDE.md).

export {};
